mixin button(obj)
    if obj.link == true
        +b('a').button( href=obj.href title=`${obj.content ? obj.content : obj.title ? obj.title : ''}` )&attributes( attributes ) #{obj.content}
            block

    else
        +b('button').button&attributes( attributes ) #{obj.content}
            block
